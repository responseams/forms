watch:
  directories:
    - src
    - tests
  fileMask: "*.php"
notifications:
  passingTests: true
  failingTests: true
phpunit:
  binaryPath: vendor/bin/pest
  arguments: "--stop-on-failure"
  timeout: 180
